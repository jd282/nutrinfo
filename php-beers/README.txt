move pdo-mine.php to the /etc/php5 folder on vm

run the setup.sh file in db-beers folder to create the nutrinfo database by typing "./setup.sh" in terminal when in correct directory

view the php files by typing "localhost/php-beers" in a browser

- to debug: go to root (su)  and then /var/log/apache2 and then type tail -f error.log to look at error messages 